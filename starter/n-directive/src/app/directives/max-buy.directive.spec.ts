import { MaxBuyDirective } from './max-buy.directive';

describe('MaxBuyDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxBuyDirective();
    expect(directive).toBeTruthy();
  });
});
