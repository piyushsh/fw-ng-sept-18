import { DoublePipe } from './double.pipe';

describe('DoublePipe', () => {

    let pipe:DoublePipe;

    beforeEach(() => {
        pipe = new DoublePipe();
    })

	it('some test', () => {
		// expect..
	});

});
